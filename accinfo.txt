module test_complex_struct_passing.c {
	projection <struct person_t> person_t_f {
		char[10] [in] name;
		projection clothes_f* [alloc(callee)] s;
	}

	projection <struct clothes> clothes_f {
		char[10] [in] color;
	}

	rpc f_void f( projection f_person_t* no_name );

	rpc main_int main(  );

}